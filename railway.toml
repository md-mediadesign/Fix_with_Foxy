[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node_modules/.bin/prisma db push --accept-data-loss && node_modules/.bin/tsx prisma/seed.ts && node_modules/.bin/next start -p ${PORT:-3000}"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
